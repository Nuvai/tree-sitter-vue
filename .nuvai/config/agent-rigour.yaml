defaults:
  rigour: low

auto_detect_hints:
  default_depth: functional
  default_strategic: competitive
  perf_critical: true
  security_sensitive: false

# Per-command overrides (uncomment to customize):
# overrides:
#   epic-plan:
#     rigour: high
#   bug-rc:
#     rigour: high
